using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace PyrrhicSilva
{
    public class HomeownerController : MonoBehaviour
    {
        // [SerializeField] protected GameManager gameManager;
        // [SerializeField] string[] startNarrationLines;
        // [SerializeField] string[] preTaskLines;
        // [SerializeField] int index;
        // [SerializeField] bool pause;
        // [SerializeField] string task = "task";

        // void Awake()
        // {
        //     if (gameManager == null)
        //     {
        //         gameManager = GameObject.FindWithTag("GameController").GetComponent<GameManager>();
        //     }
        // }
        // // Start is called before the first frame update
        // void Start()
        // {
        //     if (startNarrationLines.Length < 2)
        //     {
        //         this.gameObject.SetActive(false);
        //     }
        //     if (preTaskLines.Length < 2)
        //     {
        //         preTaskLines[0] = "Could you please ";
        //     }
        //     gameManager.PlayNarration(startNarrationLines);
        //     gameManager.StartTasks();
        // }

        // // Update is called once per frame
        // void Update()
        // {

        // }
    }
}